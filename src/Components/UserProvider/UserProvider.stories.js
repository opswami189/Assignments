/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import UserProvider from './UserProvider';

storiesOf('UserProvider', module).add('default', () => <UserProvider />);
